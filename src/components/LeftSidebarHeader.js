import React from 'react';
import styles from './LeftSidebarHeader.scss';

const leftSidebarHeader = () => {
  return (
    <div className={styles.headerContainer}>
      <span>tp</span>
    </div>
  );
};

export default leftSidebarHeader;
