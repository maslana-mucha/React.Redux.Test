import React from 'react';
import styles from './UpperMenuBarTitle.scss';

const upperMenuBarTitle = () => (
  <div className={styles.titleContainer}>
    <span>Lista specjalistów</span>
  </div>
);

export default upperMenuBarTitle;
